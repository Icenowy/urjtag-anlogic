#
# .travis.yml
# https://travis-ci.com/plans
#

language: c

sudo: false

compiler:
  - gcc

addons:
  apt:
    packages:
    - autopoint

script: |
        cd urjtag 
        ./autogen.sh
        make

# l l
